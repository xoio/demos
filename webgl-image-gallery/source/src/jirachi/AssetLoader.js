
class AssetLoader {
	constructor(images){

		this.images = [];
		this.num = images.length;
		this.loaded = 0;
	}

	startQueue(){
		this.payload = [];
		this.images.forEach(img => {

		})
	}
}
export default AssetLoader;